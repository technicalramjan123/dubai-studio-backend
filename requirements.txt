fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
sqlalchemy==2.0.30
requests==2.32.3
edge-tts==6.1.12
yt-dlp==2024.8.6

# faster-whisper: same Whisper models, but built on CTranslate2 instead
# of torch/triton. Ships prebuilt wheels (nothing to compile), much
# lighter and faster — a better fit for free-tier hosting than
# openai-whisper, which pulls in torch+triton and frequently fails to
# build on minimal containers.
faster-whisper==1.0.3
